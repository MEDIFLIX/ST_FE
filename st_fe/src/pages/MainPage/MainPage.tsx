import NavBar from '../../components/Navbar';
import SideBar from '../../components/SideBar';

const MainPage = () => {
  return (
    <div>
      <NavBar />
      <SideBar />
    </div>
  );
};

export default MainPage;
